services:
  - type: web
    name: skincare-ai
    env: python
    build:
      buildCommand: |
        pip install -r requirements.txt
        python manage.py collectstatic --noinput
    startCommand: "gunicorn -c gunicorn_config.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.13
      - key: DJANGO_SETTINGS_MODULE
        value: SkinCareProject.settings
      - key: PYTHONUNBUFFERED
        value: 1
      - key: PORT
        value: 10000
    plan: free
